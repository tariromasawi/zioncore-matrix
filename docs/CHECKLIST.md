# Reconstruction checklist

HTML is the command interface. ZionCore is the engine.
Sanctuary pages were not modified.

PASS: Dashboard, Sphere, Discovery (GitHub public FOUND), Evidence chain, Restoration pipeline labels, Orchestrator, Hallmark public key, Monitoring, Export JSON/CSV, Worker queue, Scheduler optional, Inventory boot, honest UNAVAILABLE/REQUIRES_AUTHORIZATION.
PARTIAL: Auth (local operator), GitLab/Bitbucket/Firebase (auth stubs), Ed25519 sign (private key off-repo), Docker deploy.
BLOCKED: PayPal webhooks (paid platform excluded).
