# OMNIRESTORE — MATRIX RESTORATION CORE

**This is the correct repository.**  
https://github.com/tariromasawi/zioncore-matrix

**Product:** OmniRestore Hallmarked Edition  
**Owner:** SAINT Tariro Masawi THE ANOINTED COMMANDER  
**Motto:** Mwari ndi Mwari  
**Matrix:** 77-99-33 · 777-999-333

Recover · Preserve · Verify · Restore

## Open the console (no install)

1. Open this file in the repo: [index.html](./index.html)
2. Click **Raw**, or enable GitHub Pages on `main` / root.
3. The MATRIX console runs in the browser: cases, discovery, preserve, hash chain.

## Run the Node kernel (full API)

```bash
git clone https://github.com/tariromasawi/zioncore-matrix.git
cd zioncore-matrix
node --version    # need 18+
ZIONCORE_DATA_DIR=./data npm test
PORT=5000 npm start
# then open http://127.0.0.1:5000
```

If `cd zioncore-matrix` shows nothing, you are not inside this clone. Run `ls` — you should see `index.html`, `src/`, `public/`, `package.json`.

## Parts in this repo

| Path | Role |
|---|---|
| `index.html` | Live operator console |
| `src/` | Node kernel (health, cases, evidence, hallmark) |
| `public/` | Same console served by `npm start` |
| `tests/` | Kernel tests |
| `covenant/` | Genesis seed + classified mythos |
| `keys/hallmark.pub` | Public Ed25519 hallmark |
| `matrix-index.json` | Module index |

## Honest limits

Does not search the entire internet. Does not delete third-party pages. Does not access higher dimensions. Public-web connector returns UNAVAILABLE without a licensed API.

House of Masawi · tariro@masawi.org
